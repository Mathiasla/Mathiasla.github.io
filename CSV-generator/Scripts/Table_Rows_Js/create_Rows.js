// ******* Create multiple rows **********

// SKU + Size + multiple Color
function createMultipleRowsSKUSizeMultipleColor(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesSKUSizeMultipleColor(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// SKU + Color + Multiple Size
function createMultipleRowsSKUColorMultipleSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesSKUColorMultipleSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// SKU + Multiple Size
function createMultipleRowsSKUMultipleSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesSKUMultipleSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// SKU + Multiple Color
function createMultipleRowsSKUMultipleColor(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesSKUMultipleColor(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// Name + Size + multiple Color
function createMultipleRowsNameSizeMultipleColor(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 = combineMultipleValuesNameSizeMultipleColor(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// Name + Color + Multiple Size
function createMultipleRowsNameColorMultipleSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 = combineMultipleValuesNameColorMultipleSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// Name + Multiple Color
function createMultipleRowsNameMultipleColor(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesNameMultipleColor(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// Name + Multiple Size
function createMultipleRowsNameMultipleSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesNameMultipleSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// EAN + Color + Multiple Size
function createMultipleRowsEANColorMultipleSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 = combineMultipleValuesEANColorMultipleSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// EAN + Size + Multiple Color
function createMultipleRowsEANSizeMultipleColor(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 = combineMultipleValuesEANSizeMultipleColor(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// EAN + Multiple Size
function createMultipleRowsEANMultipleSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesEANMultipleSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// EAN + Multiple Color
function createMultipleRowsEANMultipleColor(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 =combineMultipleValuesEANMultipleColor(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
// ------------------------------------------------------------
// mangler 
function createMultipleRowsSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 = combineMultipleValuesSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}
function createMultipleRowsSize(){
    // sætter tomt array til combinemultiplevalues. Efterfølgende kaldes combinemultiple array til at sætte variabel3 array til variabel1 array
   let variabel = [];
   let variabel3 = combineMultipleValuesNameSize(variabel);

   // loop start
   for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
   }
   // loop slut
}


// ******** Create Single Rows **********
// Create row : Name + Color + Size
function createRowName(){
    let variabel = [];
    let variabel3 = combineVariableName(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }  
}

// Create row : Name + Color
function createRowNameColor(){
    let variabel = [];
    let variabel3 = combineVariableNameColor(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }  
}

// Create Row : Name + Size
function createRowNameSize(){
    let variabel = [];
    let variabel3 = combineVariableNameSize(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }  
}
// Create Row : SKU + Color + Size
function createRowSKU(){
    let variabel = [];
    let variabel3 = combineVariableSKU(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : SKU + Size
function createRowSKUSize(){
    let variabel = [];
    let variabel3 = combineVariableSKUSize(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : SKU + Color
function createRowSKUColor(){
    let variabel = [];
    let variabel3 = combineVariableSKUColor(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : EAN + Color + Size
function createRowEAN(){
    let variabel = [];
    let variabel3 = combineVariableEAN(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : EAN + Color
function createRowEANColor(){
    let variabel = [];
    let variabel3 = combineVariableEANColor(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : EAN + Size
function createRowEANSize(){
    let variabel = [];
    let variabel3 = combineVariableEANSize(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : EAN + Color + Brand
function createRowEANColorBrand(){
    let variabel = [];
    let variabel3 = combineVariableEANColorBrand(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }  
}
// Create Row : EAN + Color + Brand
function createRowEANSizeBrand(){
    let variabel = [];
    let variabel3 = combineVariableEANSizeBrand(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }  
}
// Create Row : SKU + Color + Brand + Product_Cost
function createRowAdditionalAttributeEANColorBrandProductCost(){
    let variabel = [];
    let variabel3 = combineVariableEANColorBrandProductCost(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}

// Create Row : SKU + Color + Size SKU
function createRowSKUSKU(){
    let variabel = [];
    let variabel3 = combineVariableSKUSKU(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : SKU + Size SKU
function createRowSKUSizeSKU(){
    let variabel = [];
    let variabel3 = combineVariableSKUSizeSKU(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : SKU + Color SKU
function createRowSKUColorSKU(){
    let variabel = [];
    let variabel3 = combineVariableSKUColorSKU(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}



// Create Row : SKU + Size + Brand + Product_Cost
function createRowAdditionalAttributeSKUSizeBrandProductCost(){
    let variabel = [];
    let variabel3 = combineVariableEANSizeBrandProductCost(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : SKU + Color + Brand + Product_Cost
function createRowAdditionalAttributeSKUColorBrandProductCost(){
    let variabel = [];
    let variabel3 = combineVariableEANSizeBrandProductCost(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}
// Create Row : SKU + Size + Color + Brand + Product_Cost
function createRowAdditionalAttributeSKUSizeColorBrandProductCost(){
    let variabel = [];
    let variabel3 = combineVariableEANSizeColorBrandProductCost(variabel);

    for(let key in variabel3) {
        var table = document.getElementById("myTable");
        var row = table.insertRow();
        var cell = row.insertCell();
        cell.innerHTML = variabel3[key];
    }
}